# 字段校验与错误映射

## 目的

统一记录 OA 审批相关字段校验规则与错误返回约定，便于前后端协作与问题定位。

## 核心校验

- 报销类型：仅允许“差旅 / 团建”
- 金额：必须大于或等于 0
- 表单数据：必须为有效 JSON
- 附件：数量与大小限制由业务配置决定

## 错误映射

- 400 BAD_REQUEST：请求参数缺失或格式错误
- 401 UNAUTHORIZED：未登录或用户失效
- 404 NOT_FOUND：资源不存在
- 500 ERROR：服务内部错误
