【文图一致性自检报告 - 凭证录入页】
生成时间：2026-04-04
文档版本：V1.0.0

═══════════════════════════════════════════════════════════

A. 工具栏按钮
   MD定义：4组按钮 → HTML实现：4组按钮
   □ 清空               ✅  （onclick="confirmClear()"，弹通用确认弹窗后重置）
   □ 新增▼（分裂按钮）  ✅  （主按钮 onclick="doNewVoucher()"；箭头展开下拉）
   □ 保存▼（分裂按钮）  ✅  （主按钮蓝色 onclick="doSave()"；箭头展开下拉）
   □ 提交               ✅  （btn-primary蓝色，onclick="doSubmit()"）
   顺序：清空→新增▼→保存▼→提交  ✅

   A1. 新增▼ 下拉菜单（MD定义：3项）
   □ 新增（高亮 active-item）  ✅
   □ 引入常用凭证              ✅  （onclick="openModal('commonModal')"）
   □ 引入草稿凭证              ✅  （onclick="openModal('draftModal')"）

   A2. 保存▼ 下拉菜单（MD定义：4项）
   □ 保存           ✅
   □ 保存新增        ✅  （onclick="doSaveNew()"，600ms后重置表单）
   □ 保存为草稿      ✅  （Toast占位提示）
   □ 保存为常用凭证  ✅  （onclick="openModal('saveTemplateModal')"）

B. 凭证头部字段
   MD定义：4个字段 → HTML实现：4个字段
   □ *日期（必填星号，type=date，id=veDateInput）      ✅
   □ *凭证字（必填星号，select，id=veVoucherWord）     ✅  （默认spt模式：收/付/转）
   □ 凭证号（text，id=veVoucherNo，4位限制）           ✅
   □ 附件数（number，id=veAttachCount，min=0）         ✅
   □ 凭证字切换→中央标题联动（syncVoucherWord）        ✅

C. 分录明细列（及合计行）
   MD定义：6列 → HTML实现：6列
   □ 序号          ✅  （col-seq，点击展开行菜单，hover显示⚙）
   □ 摘要          ✅  （col-summary，inline input + 自动补全）
   □ 科目名称      ✅  （col-subject，inline input + 科目自动补全）
   □ 辅助核算项目  ✅  （col-aux，button触发浮层）
   □ 借方金额      ✅  （col-amt，number input，右对齐，实时更新合计）
   □ 贷方金额      ✅  （col-amt，number input，右对齐，实时更新合计）
   □ 合计行        ✅  （colspan=4"合计" + id=veTotalDebit + id=veTotalCredit）

D. 弹窗

   【H1. 常用凭证参照弹窗（commonModal）】
   □ 标题：常用凭证参照                                ✅
   □ 过滤栏：查询类型下拉+关键字输入+过滤按钮+结果中过滤复选框+栏目按钮  ✅
   □ 表格列：序号/编码/说明/凭证类别/附单据数/所属分类  ✅
   □ 预置数据：14条（销售5条+采购3条+费用2条+薪资2条+补充4条）        ✅  （已修正：原10条→14条）
   □ 单击高亮/双击引入                                 ✅
   □ 底部信息"共N条记录"                              ✅
   □ 按钮：确定/取消                                   ✅

   【H2. 草稿凭证弹窗（draftModal）】
   □ 标题：草稿                              ✅
   □ 表格列：序号/□（全选）/单据编号/凭证类别/制单日期/附单据数  ✅
   □ 空状态：暂无草稿凭证                    ✅
   □ 按钮：删除（disabled红色）/确定/取消    ✅

   【H3. 保存为常用凭证弹窗（saveTemplateModal）】
   □ 标题：保存为常用凭证             ✅
   □ *编码（必填，maxlength=10）      ✅
   □ *说明（必填，maxlength=50）      ✅
   □ *所属分类（必填，select：销售/采购/费用/薪资/其他）  ✅
   □ 凭证类别（只读，联动当前凭证字）  ✅
   □ 按钮：取消/确定                  ✅

   【H4. 通用确认弹窗（confirmModal）】
   □ 动态标题（id=confirmTitle）      ✅
   □ 动态正文（id=confirmMsg）        ✅
   □ 按钮：确定（含回调）/取消        ✅
   □ 遮罩层点击关闭                   ✅

E. 示例数据
   □ 行数一致（MD：10行空白分录 / HTML：10行初始化） ✅
   □ 初始化内容（summary/subject/auxVals/debit/credit均为空/0）  ✅

F. 分页控件
   □ MD不需要分页 → HTML无分页控件  ✅

G. 标签页
   □ MD无标签页 → HTML无标签页      ✅

H. 交互实现
   □ 科目自动补全（showSubjAC/selectSubject/hideSubjAC）    ✅
   □ 摘要自动补全（showSumAC/selectSummary/hideSumAC）      ✅
   □ 辅助核算浮层（showAuxPopup/clearAux，多选+清除全部）   ✅
   □ 行右键菜单（showRowMenu：增行/插行/复制行/删行）       ✅
   □ 删行后行数为0自动补1行                                 ✅
   □ 实时合计更新（updateTotals，oninput触发）              ✅
   □ 提交校验（≥2有效分录行 + 借贷平衡误差≤0.001）         ✅
   □ Toast三色提示（success绿/error红/warning黄）           ✅
   □ 清空→通用确认弹窗→resetForm→Toast"已清空"            ✅
   □ 新增→通用确认弹窗→resetForm→Toast"已新建凭证"        ✅
   □ 保存→Toast"凭证XXX已保存，状态：待审核"               ✅
   □ 保存新增→保存后600ms重置                              ✅
   □ 凭证字切换→中央标题/常用凭证类别同步更新               ✅
   □ 下拉菜单点击其他区域关闭（document.onclick）           ✅

═══════════════════════════════════════════════════════════
自检结论：
❌ 发现 1 处不一致，已修正如下：
   - 常用凭证参照弹窗预置数据：HTML原有10条 → MD定义14条 → 已补充4条（日常费用报销/计提折旧/计提工资税款/代扣个人所得税），修正后14条一致 ✅

修正后全部一致，可交付 ✅

交付物清单：
  ├── V1.0.0-凭证录入页.md            （需求文档 V1.0.0）
  ├── 凭证录入页-低保真原型.html       （交互原型 V1.0.0）
  └── 凭证录入页-一致性自检报告.txt    （本文件）
═══════════════════════════════════════════════════════════
