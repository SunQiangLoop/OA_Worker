【文图一致性自检报告 - 凭证审核页】
生成时间：2026-04-04
文档版本：V1.2.0

═══════════════════════════════════════════════════════════

A. 工具栏按钮
   MD定义：12个 → HTML实现：12个
   □ 反审核          ✅  （id="btn-unaudit"，默认禁用，文字一致）
   □ 审核            ✅  （id="btn-audit"，默认禁用，文字一致）
   □ 出纳复核        ✅  （id="btn-cashier"，默认禁用，文字一致）
   □ 取消复核        ✅  （id="btn-uncheck"，默认禁用，文字一致）
   □ 记账            ✅  （id="btn-post"，默认禁用，文字一致）
   □ 取消记账        ✅  （id="btn-unpost"，默认禁用，文字一致）
   □ 删除            ✅  （id="btn-delete"，默认禁用，文字一致）
   □ 操作▼           ✅  （dropdown-wrap，始终可用，下拉含5条菜单项）
   □ 栏目            ✅  （始终可用，点击打开栏目设置弹窗）
   □ 导出            ✅  （id="btn-export"，默认禁用，文字一致）
   □ 推送            ✅  （id="btn-push"，class="btn-push"，橙色样式，默认禁用）
   □ 🖨（打印）      ✅  （id="btn-print"，默认禁用，文字一致）
   顺序：反审核→审核→出纳复核→取消复核→记账→取消记账→删除→操作▼→栏目→导出→推送→🖨  ✅

B. 列表表头字段
   MD定义：15列 → HTML实现：15列
   □ □（全选复选框）  ✅  （col-cb，input[type=checkbox] id="check-all"）
   □ 序号             ✅  （col-seq，居中）
   □ 制单日期 ▲      ✅  （col-date，含排序标记）
   □ 凭证字号 ▲      ✅  （col-id，含排序标记）
   □ 凭证总金额       ✅  （col-amt，右对齐加粗）
   □ 审核人           ✅  （col-auditor）
   □ 制单人           ✅  （col-maker）
   □ 摘要             ✅  （col-summary，white-space:normal，最大宽度220px）
   □ 外部系统数据单号 ✅  （col-extdoc，仅首行分录显示单号链接）
   □ 状态             ✅  （col-status，彩色标签居中）
   □ 科目编码         ✅  （col-code）
   □ 科目名称         ✅  （col-name）
   □ 辅助项           ✅  （col-aux）
   □ 借方             ✅  （col-dr，右对齐）
   □ 贷方             ✅  （col-cr，右对齐）
   未添加 MD 未定义的列  ✅

C. 筛选条件
   MD定义：双行8个控件 → HTML实现：双行8个控件
   第一行：
   □ 凭证类别（文本输入框）  ✅  （id="voucher-type-keyword"）
   □ 制单日期范围（默认选中单选按钮 + 日期范围）  ✅  （id="vaDateStart"/"vaDateEnd"）
   □ 摘要（文本输入框）      ✅  （id="voucher-filter-summary"）
   □ 金额范围（数字范围）    ✅  （id="voucher-amount-min"/"voucher-amount-max"）
   第二行：
   □ 科目编码（文本输入框）  ✅  （id="voucher-subject-code"）
   □ 会计期间（默认未选单选按钮 + select下拉范围）  ✅  （id="vaPeriodStart"/"vaPeriodEnd"，默认disabled）
   □ 单号智能查询（文本输入框）  ✅  （id="voucher-smart-search"，placeholder含 YD/BX/PC 说明）
   □ 查询（按钮）            ✅  （onclick="applySearch()"）
   □ 制单日期/会计期间互斥逻辑  ✅  （switchDateMode() 函数）

D. 弹窗/抽屉
   【E1. 出纳复核弹窗】
   MD定义：居中弹窗 → HTML实现：modal-overlay id="cashier-modal"
   □ 标题：出纳复核                    ✅
   □ 正文：确认进行出纳签字吗？        ✅
   □ 按钮1：取消（关闭弹窗）           ✅
   □ 按钮2：确认（写入cashierUser，状态变已复核）  ✅
   □ 点击遮罩层关闭弹窗               ✅

   【E2. 操作下拉菜单（5条）】
   MD定义：5条菜单项 → HTML实现：5条
   □ 作废（红色）        ✅
   □ 取消作废（默认）    ✅
   □ 冲销（红色）        ✅
   □ 标错（红色）        ✅
   □ 取消标错（默认）    ✅
   □ 作废/冲销含confirm确认  ✅

   【E3. 栏目设置弹窗（27列）】
   MD定义：27条 → HTML实现：27行（序号1-27）
   □ 标题：栏目设置                                ✅
   □ 弹窗尺寸：width=620px                        ✅  （modal-box-xl）
   □ 定位搜索框                                    ✅  （col-search）
   □ 表头类型列（15条白色）                        ✅  （制单日期/凭证字号/附单据数/来源类型/差异凭证/错误/作废/凭证总金额/现金流量分配/记账人/审核人/出纳/制单人/打印次数/附件数 + 状态）
   □ 明细类型列（11条蓝色）                        ✅  （摘要/科目编码/科目名称/辅助项/计量单位/数量/单价/借方/贷方/来源类型备注/外部系统数据单号）
   □ 每列属性：序号/显示/固定列/栏目名称/类型/排序/合计  ✅
   □ 顺序调整按钮：置顶/上移/下移/置底            ✅
   □ 底部选项：显示明细 / 只显示第一行明细        ✅
   □ 注：调整内容只应用于当前用户。               ✅
   □ 底部按钮：恢复默认 / 取消 / 确定             ✅

   【E4. 关联原单弹窗】
   MD定义：居中弹窗700px，7列明细表 → HTML实现：id="sdoc-overlay"，modal-box-lg
   □ 标题：关联原单明细                           ✅
   □ 副标题：凭证号：XXX · 原单类型               ✅  （id="sdoc-subtitle"）
   □ 弹窗宽度：700px（max 92vw），max-height 80vh  ✅
   □ 明细表列：序号/单号/日期/线路货物/金额/司机相关人/状态  ✅
   □ 关闭方式：右上角×按钮 + 遮罩层点击           ✅
   □ 空状态：暂无关联明细                         ✅
   □ 含示例数据（v001：RC运单，v002：SET结算单+RC运单）  ✅

E. 示例数据
   □ 行数一致（MD：5行 / HTML：5行）              ✅
   □ 凭证组1：记-3526 × 3行，状态"待审核"         ✅
   □ 凭证组2：收-0001 × 2行，状态"已记账"         ✅
   □ 记-3526 第1行外部单号：RC20260404171750002    ✅
   □ 收-0001 第1行外部单号：SET-202682252          ✅
   □ 其余行外部单号列为空                         ✅
   □ 审核人：记-3526 显示"-"，收-0001 显示"系统审核"  ✅
   □ 制单人：记-3526 为"系统"，收-0001 为"核销确认"   ✅

F. 分页控件
   MD定义：有（30条/页）→ HTML实现：有
   □ 首页 / 上一页 / 数字页码 / 下一页 / 末页  ✅
   □ 跳至输入框 + GO 按钮                      ✅
   □ 总页数（/ N 页）                          ✅  （"/ 1 页"）
   □ 底部左侧：□ 未取已签合计                  ✅

G. 标签页（5个）
   MD定义：5个 → HTML实现：5个
   □ 全部凭证（默认激活）  ✅
   □ 待审核                ✅
   □ 待出纳复核            ✅
   □ 待记账                ✅
   □ 差异凭证              ✅
   □ 点击切换逻辑（过滤对应状态行）  ✅

H. 交互实现
   □ 勾选分录行同凭证多行联动                   ✅  （onRowCheck / 按 data-vid 匹配）
   □ 表头全选复选框控制所有行                   ✅  （toggleAll）
   □ 选中凭证后操作按钮从禁用变可用             ✅  （updateActionBtns）
   □ 操作▼下拉展开/收起（点击其他区域关闭）     ✅
   □ 出纳复核弹窗：取消关闭 / 确认后关闭+提示   ✅
   □ 删除：confirm 确认文案正确                 ✅
   □ 作废：confirm 确认后执行                   ✅
   □ 冲销：confirm 确认后执行                   ✅
   □ 关联原单弹窗：含示例数据，点击遮罩关闭      ✅
   □ 栏目设置弹窗：27行，含底部操作             ✅
   □ 制单日期/会计期间互斥（switchDateMode）    ✅
   □ 状态标签彩色（8种CSS类）                   ✅
   □ 未选中时点击操作按钮 → alert"请先选择凭证。"  ✅

═══════════════════════════════════════════════════════════
自检结论：
✅ 全部一致，可交付

交付物清单：
  ├── V1.0.0-凭证审核页.md        （需求文档 V1.2.0）
  ├── 凭证审核页-低保真原型.html   （交互原型 V1.2.0）
  └── 凭证审核页-一致性自检报告.txt （本文件）
═══════════════════════════════════════════════════════════
